# Copy to bootstrap/aws-credentials.ini and fill in the key material.
# bootstrap/aws-credentials.ini is gitignored -- never commit real keys.
#
# These are the credentials for the dedicated Crossplane IAM user, NOT your SSO
# session. The provider needs long-lived static keys; it cannot refresh an SSO
# token. Account: 022334369124, region us-west-2.
[default]
aws_access_key_id = <PASTE CREDS HERE>
aws_secret_access_key = <PASTE CREDS HERE>
