apiVersion: v1
kind: Namespace
metadata:
  name: aws-lab
  # Deliberately NOT labelled istio-injection=enabled. Provider pods run in
  # crossplane-system, but keeping this namespace out of the mesh avoids any
  # sidecar interference with MR reconciliation.
